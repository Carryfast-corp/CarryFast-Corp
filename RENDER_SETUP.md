# Render Setup

Render is no longer required for this project.

The app is now a frontend-only React application using Firebase Auth, Firestore, and Firebase Storage directly from the frontend.

Deploy the `frontend` directory to a static host such as Vercel, Firebase Hosting, Netlify, or Render Static Sites.
